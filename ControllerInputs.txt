
Controller Stuff: 

    access controller by gamepad1 or gamepad2

    (gamepad1.a)

    (gamepad1.b)

    (gamepad1.x)

    (gamepad1.y)

    (gamepad1.right_trigger) 

    (gamepad1.left_trigger)

    (gamepad1.right_bumper)

    (gamepad1.left_bumper)

    (gamepad1.dpad_left)

    (gamepad1.dpad_right)

    (gamepad1.dpad_up)

    (gamepad1.dpad_down)

Servo Stuff:

    setPosition() -- accepts values between 0 & 1
                   
Motor Stuff:

    setTargetPosition()
    setPower()
    setMotorSpeed() -- accepts vlaues greater than 0
    MOTORNAME.setDirection(DcMotor.Direction.FORWARD);
    MOTORNAME.setDirection(DcMotor.Direction.REVERSE);

Defining Hardware:
    Ex:
    leftDrive  = hardwareMap.get(DcMotor.class, "left_front_drive"); 
    
    name = hardwareMap.get(Typeofthing.class, "Name in config");